# docker-compose.yml
version: '2.2'
services:
  web:
    image: october:latest
    container_name: october
    ports:
      - 8000:80
    environment:
      # - APP_URL=https://octobercms.com/
      # - LINK_POLICY=secure
